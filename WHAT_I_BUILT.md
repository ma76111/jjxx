# ما تم بناؤه — شرح بسيط

## المشروع باختصار

بوت تيليجرام + موقع ويب لتبادل المهام المدفوعة.

الفكرة: شخص عنده بوت يريد مشتركين → يدفع مال أو نقاط مقايضة → أشخاص آخرون ينفذون المهمة ويأخذون المكافأة.

---

## الأجزاء الثلاثة

### 1. بوت تيليجرام (`index.js`)
- يعمل على Node.js
- المستخدم يتفاعل معه من تيليجرام مباشرة
- يحفظ كل البيانات في ملف `bot.db` (قاعدة بيانات SQLite)

### 2. سيرفر API (`web/server/`)
- يعمل على Express.js على البورت 3001
- يقرأ ويكتب من نفس `bot.db` — قاعدة بيانات واحدة مشتركة
- يوفر الـ API للموقع

### 3. موقع الويب (`web/client/`)
- React + Vite + TailwindCSS
- يعمل على البورت 5173 في التطوير
- لوحة تحكم للمستخدمين والأدمن

---

## كيف يسجّل الدخول للموقع

بدون كلمة سر — كل شيء عبر تيليجرام:

1. تفتح الموقع → تضغط "افتح البوت"
2. تفتح البوت في تيليجرام → تضغط Start
3. تشارك رقم هاتفك (مرة واحدة فقط)
4. الموقع يكتشف تلقائياً أنك وافقت → يدخلك

---

## ما يقدر المستخدم يعمله

- **ينشئ مهمة**: يحدد كم شخص يريد، المكافأة، نوع الإثبات (نص/صور)
- **ينفذ مهام**: يثبت تنفيذه ويأخذ المكافأة
- **يودع ويسحب**: عبر USDT (Binance Pay أو TXID)
- **يفتح تذاكر دعم**: محادثة مباشرة مع الأدمن
- **يرى إشعاراته**: في شريط علوي على الموقع وفي البوت

---

## ما يقدر الأدمن يعمله

- يراجع ويقبل/يرفض: الإيداعات، السحوبات، الإثباتات، الاستئنافات
- يبحث عن أي مستخدم ويعدل رصيده أو نقاطه أو يحظره
- يرى سجل الأجهزة والحسابات المكررة
- يتحكم في إعدادات النظام كلها (حدود السحب، المكافآت، إلخ)
- يتحكم في نظام المخالفات (نقاط، عتبات، عقوبات)
- أي إجراء خطير (تعديل رصيد كبير، حظر دائم) يحتاج موافقة أدمن ثانٍ (Maker-Checker)

---

## نظام المخالفات

مسارين منفصلين:

**مسار الثقة** — للأخطاء العادية (رفض إثبات، بلاغات)
- عقوبات تدريجية: تحذير → مراجعة إجبارية → حد سحب → منع إنشاء مهام

**مسار الأمان** — للاحتيال (حسابات مكررة، محاولات غش)
- عقوبات أقوى: حظر مؤقت → تجميد → انتظار قرار حظر دائم

---

## نظام مكافحة الاحتيال

- أول ما يفتح أي مستخدم البوت، تظهر له رسالة تحذيرية في الإشعارات (بدون إلزام موافقة)
- لو شخص فتح أكثر من حساب على نفس الجهاز → لا يقدر ينفذ نفس المهمة من الحسابين
- كل 30 دقيقة النظام يفحص تلقائياً ويضيف مخالفات للمشبوهين
- البوت يتعرف على الجهاز عبر "بصمة" = IP + نوع الجهاز

---

## الـ Dark Mode

- زر 🌙/☀️ في الشريط العلوي للموقع
- يتذكر اختيارك في الزيارة القادمة
- يحترم إعداد جهازك تلقائياً في أول زيارة

---

## قاعدة البيانات

ملف واحد `bot.db` يحتوي على:

| الجدول | ماذا يخزن |
|---|---|
| `users` | كل المستخدمين وأرصدتهم |
| `tasks` | المهام المنشورة |
| `task_submissions` | الإثباتات المقدمة |
| `deposits` / `withdrawals` | طلبات الإيداع والسحب |
| `violations` | مخالفات المستخدمين |
| `bans` | سجل الحظر |
| `tickets` | تذاكر الدعم |
| `notifications` | الإشعارات |
| `device_logs` | سجل الأجهزة |
| `fraud_task_log` | سجل كشف الاحتيال |
| `login_sessions` | جلسات تسجيل الدخول للموقع |

---

## تشغيل المشروع

```
1. ضع بيانات البوت في .env و web/server/.env
2. شغّل start.bat
   - نافذة زرقاء: البوت
   - نافذة صفراء: السيرفر (port 3001)
   - نافذة بنفسجية: الموقع (port 5173)
3. افتح http://localhost:5173
```

---

## الملفات المهمة

```
index.js              ← نقطة بدء البوت
web/server/index.js   ← نقطة بدء السيرفر
web/client/src/App.tsx ← نقطة بدء الموقع
.env                  ← توكن البوت والإعدادات
bot.db                ← قاعدة البيانات (تُنشأ تلقائياً)
start.bat             ← تشغيل كل شيء دفعة واحدة
```
